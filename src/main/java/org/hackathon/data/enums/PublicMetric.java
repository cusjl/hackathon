package org.hackathon.data.enums;

/** Only these aggregate metrics can be published. Admin data is never configurable here. */
public enum PublicMetric {
    PARTICIPANT_COUNT,
    CAMPUS_DISTRIBUTION,
    TEAM_COUNT,
    SUBMISSION_COUNT,
    TRACK_SUBMISSIONS,
    AI_TOOLS,
    TECH_STACKS
}
